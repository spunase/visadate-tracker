/**
 * Barrel export for all custom hooks.
 *
 * Import hooks from "@/lib/hooks" instead of deep paths.
 *
 * @module hooks
 */

export { useTheme } from "@/components/theme-provider";
export type { Theme, ThemeContextValue } from "@/components/theme-provider";
